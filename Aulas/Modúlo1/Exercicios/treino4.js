const prompt = require('prompt-sync')();

let opções = ['NEGADO', 'OPCIONAL', 'OBRIGATORIO'];

let resultado = opções[Math.floor(Math.random() * opções.length)];

for (let i = 0; i < 1; voto() ){
i++
function voto(){
    var ano = prompt("digite o ano do seu nascimento: ");
    console.log(`nestas eleições, vc tem o voto ${resultado}`);
}
}